# NOTE: PyTorch must be installed manually with CUDA support.
# Run: python tools/diagnostics/gpu_setup.py
# Do NOT run plain: pip install torch (installs CPU version)

# ─── Core Dependencies ───
faster-whisper
edge-tts
pyyaml
python-dotenv
pydantic
tzdata
structlog

# ─── LLM Backends ───
google-genai            # NEW: replaces deprecated google-generativeai
groq
ollama

# ─── Memory ───
chromadb>=0.4.24
sentence-transformers
psutil
nvidia-ml-py>=12.0
wmi>=1.5.1

# ─── Audio Pipeline ───
sounddevice
soundfile
numpy
torch  # installed via CUDA wheel — see gpu_setup.py for install command
torchaudio

# ─── PC Control & Input ───
pyautogui
pillow

# ─── Audio Playback ───
pygame-ce

# ─── Volume Control (optional but recommended) ───
pycaw
comtypes

# ─── Tool Safety ───
jsonschema
fastapi
uvicorn

# ─── Fuzzy Matching ───
rapidfuzz
yt-dlp

# ─── Document Parsing (Optional) ───
python-docx
pypdf
openpyxl

# ─── MCP Server ───
fastmcp
pywin32
